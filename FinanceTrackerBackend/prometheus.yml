global:
  scrape_interval: 5s

scrape_configs:
  - job_name: 'FinanceTrackerBackend'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: [app:8080]  # or 'localhost:8080' if not in Docker
